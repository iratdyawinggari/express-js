let VehicleRepository;_b2f‍.x([["default",()=>VehicleService]]);_b2f‍.w("../repository/vehicle.repository",[["default",["VehicleRepository"],function(v){VehicleRepository=v}]]);

               class VehicleService {
    async getVehicleById(id) {
        return await new VehicleRepository().findOne(id);
    }

    async createVehicle(vehicle) {
        return await new VehicleRepository().createVehicle(vehicle)
    }

    async getAllVehicle() {
        return await new VehicleRepository().findAllVehicle()
    }
   
}