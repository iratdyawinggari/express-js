let SlotRepository;_70b‍.x([["default",()=>SlotService]]);_70b‍.w("../repository/slot.repository",[["default",["SlotRepository"],function(v){SlotRepository=v}]]);

               class SlotService {
    async getSlotById(id) {
        return await new SlotRepository().findOne(id);
    }

    async createSlot(slot) {
        return await new SlotRepository().createSlot(parking)
    }

    async getAllSlot() {
        return await new SlotRepository().findAllSlot()
    }
   
    async updateSlot(slot){
        return await new SlotRepository().updateSlot(slot)
    }
}