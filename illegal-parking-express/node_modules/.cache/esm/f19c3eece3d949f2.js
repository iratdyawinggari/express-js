let Router,VehicleService;_e3d‍.x([["default",()=>_e3d‍.o]]);_e3d‍.w("express",[["Router",["Router"],function(v){Router=v}]]);_e3d‍.w("../services/vehicle.service",[["default",["VehicleService"],function(v){VehicleService=v}]]);


const VehicleRouter = Router()
    .get('/', async (req, res, next) => {
        const rows = await new VehicleService().getAllVehicle()
        res.json(rows)
    })
    .post('/', async (req, res, next) => {
        const vehicle = { ...req.body }
        const rows = await new VehicleService().createVehicle(vehicle)
        if (rows) {
            res.json(rows)
        } else {
            res.json({ message: 'null' })
        }
    })
    .get('/id', async (req, res, next) => {
        const rows = await new VehicleService().getVehicleById(Number(req.query.id))
        res.json(rows)
    })

_e3d‍.d(VehicleRouter);