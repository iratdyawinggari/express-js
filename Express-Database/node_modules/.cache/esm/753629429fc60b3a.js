let getRepository,Category;_9fc‍.x([["default",()=>CategoryRepository]]);_9fc‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_9fc‍.w("../models/category.model",[["default",["Category"],function(v){Category=v}]]);


               class CategoryRepository{
    categoryRepository(){
        return getRepository(Category)
    }

    async findOne(id){
        const category = await this.categoryRepository().find({where:{id:id}},{relations:['product']})
        return category
    }

    async findAllCategory(){
        const category = await this.categoryRepository().find()
        return category
    }

    async findAllCategoryProduct(){
        const category = await this.categoryRepository().find({relations:['product']})
        return category
    }

    async createCategory(category){
        return await this.categoryRepository().save(category)
    }    
}