let getRepository,User;_367‍.x([["default",()=>UserRepository]]);_367‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_367‍.w("../models/user.model",[["default",["User"],function(v){User=v}]]);


               class UserRepository{
    userRepository(){
        return getRepository(User)
    }

    async findOne(id){
        const user = await this.userRepository().find({where:{id:id}},{relations:['userInfo']})
        return user
    }

    async findAllUser(){
        const user = await this.userRepository().find()
        return user
    }

    async findAllUserUserInfo(){
        const user = await this.userRepository().find({relations:['userInfo']})
        return user
    }

    async createUser(user){
        return await this.userRepository().save(user)
    }    
}