let Router,UserService;_176‍.x([["default",()=>_176‍.o]]);_176‍.w("express",[["Router",["Router"],function(v){Router=v}]]);_176‍.w("../services/user.service",[["default",["UserService"],function(v){UserService=v}]]);


const UserRouter = Router()
    .get('/id', async (req, res, next) => {
        const rows = await new UserService().getUserById(Number(req.query.id))
        res.json(rows)
    })
    .post('/', async (req, res, next) => {
        const user = { ...req.body }
        const rows = await new UserService().createUser(user)
        if (rows) {
            res.json(rows)
        } else {
            res.json({ message:'null' })
        }
    })
    .get('/', async (req, res, next) => {
        const rows = await new UserService().getAllUser()
        res.json(rows)
    })

_176‍.d(UserRouter);